#ifndef REVERSESTRING_H
#define REVERSESTRING_H

/*
 this function will take the inputs of string and length of the string
*/
void string_reverse(char [],int);

/*
this variable will contain the reversed string
*/
char rev[10000];

#endif // REVERSESTRING_H