#include <string.h>
#include <stdio.h>
#include "./reverseString.h"

void main(){
    char main_string[100];
    printf("enter a string = ");
    gets(main_string);
    string_reverse(main_string,strlen(main_string));
    printf(rev);
}
